class Solution {
    public int largestAltitude(int[] gain) {
        int max = 0;
        int pre = 0;
        for(int i=0; i< gain.length; i++){
            pre += gain[i];
            max = Math.max(pre, max);
        }
        return max;
    }
}
