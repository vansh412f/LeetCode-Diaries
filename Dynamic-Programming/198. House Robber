class Solution {
    public int rob(int[] nums) {
        int prev = 0;
        int max = 0;
        for(int num: nums){
            int temp = Math.max(prev + num, max);
            prev = max;
            max = temp;
        }
        return max;
    }
}
