class Solution {
    public int[] minBitwiseArray(List<Integer> nums) {
        int n = nums.size();
        int[] ans = new int[n];
        for (int i = 0; i < n; i++) {
            int a = nums.get(i);
            if (nums.get(i) % 2 == 0) {
                ans[i] = -1;
            } else {
                ans[i] = a - ((a + 1) & (-a - 1)) / 2;
            }
        }
        return ans;

    }
}
